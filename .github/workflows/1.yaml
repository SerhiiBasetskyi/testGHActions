name: Deploy AAA to BBBB

on:
  workflow_dispatch:
    inputs:
      enable_param1:
        type: boolean
        description: 'Enable param1'
        default: false
      enable_param2:
        type: boolean
        description: 'Enable param2'
        default: false
      enable_param3:
        type: boolean
        description: 'Enable param3'
        default: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        param_name: ['param1', 'param2', 'param3']
    name: Deploy with Parameter ${{ matrix.param_name }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v2
      
      - name: Set Parameter Enablement
        id: set_parameter_enablement
        run: |
          echo "enable_param1=${{ github.event.inputs.enable_param1 }}" >> $GITHUB_ENV
          echo "enable_param2=${{ github.event.inputs.enable_param2 }}" >> $GITHUB_ENV
          echo "enable_param3=${{ github.event.inputs.enable_param3 }}" >> $GITHUB_ENV
      
      - name: Deploy with param1
        if: matrix.param_name == 'param1' && env.enable_param1 == 'true'
        run: |
          echo "Deploying with param1"
          # Add your deployment script for param1 here

      - name: Deploy with param2
        if: matrix.param_name == 'param2' && env.enable_param2 == 'true'
        run: |
          echo "Deploying with param2"
          # Add your deployment script for param2 here

      - name: Deploy with param3
        if: matrix.param_name == 'param3' && env.enable_param3 == 'true'
        run: |
          echo "Deploying with param3"
          # Add your deployment script for param3 here